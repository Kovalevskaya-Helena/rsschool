export type LoadStatus = 'pending' | 'loading' | 'loaded' | 'error';

export interface Items {
  name: string;
  gender: string;
  birth_year: string;
  height: string;
  mass: string;
  eye_color: string;
  url: string;
}
