export type LoadStatus = 'pending' | 'loading' | 'loaded' | 'error';
export interface Items {
  name: string;
  gender: string;
  birthYear: string;
  height: string;
  mass: string;
  eyeColor: string;
  url: string;
}
